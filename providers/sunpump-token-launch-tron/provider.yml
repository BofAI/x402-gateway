name: sunpump-token-launch-tron
title: "SunPump Token Launch (TRON Mainnet)"
description: "Configured SunPump launch route; upstream POST currently returns 405"
category: finance
version: v1

forward_url: https://sunpump.meme

display:
  service_url: https://tm-x402-gateway.bankofai.io/providers/sunpump-token-launch-tron
  tags: ["sunpump", "token-launch", "tron"]

discovery:
  use_case: "Inspect the configured SunPump x402 route on TRON Mainnet while the upstream launch API is unavailable."
  spend_aware_usage:
    - "Use a low fixed TRON Mainnet USDT payment per request."
  when_to_use:
    - "Payment-route integration checks while SunPump upstream POST returns 405"
  request_examples:
    - "POST /providers/sunpump-token-launch-tron/pump-api/ai/agentTokenLaunch"

routing:
  type: proxy

operator:
  network: tron-mainnet
  currencies:
    usd: ["USDT"]
  recipient: TLXPgJVJFgL97gc49j8w8kC22mDTpH9EGa
  scheme: exact
  protocol: exact
  asset_transfer_method: permit2
  facilitator_url: https://facilitator-v2.bankofai.io
  facilitator_api_key: 2125fd45151833cfb529d1e77885fd46a1244966bc4e83add35b40ba494ea89d
  valid_for_seconds: 300

endpoints:
  - method: POST
    path: /pump-api/ai/agentTokenLaunch
    description: "Configured SunPump launch route; upstream POST currently returns 405"
    metering:
      dimensions:
        - direction: usage
          unit: requests
          scale: 1
          tiers:
            - price_usd: 0.000001
