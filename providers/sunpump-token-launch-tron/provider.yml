name: sunpump-token-launch-tron
title: "SunPump Token Launch (TRON Nile)"
description: "Configured SunPump launch route; upstream POST currently returns 405"
category: finance
version: v1

forward_url: https://sunpump.meme

display:
  service_url: https://tm-x402-gateway.bankofai.io/providers/sunpump-token-launch-tron
  tags: ["sunpump", "token-launch", "tron"]

discovery:
  use_case: "Inspect the configured SunPump x402 route on TRON Nile while the upstream launch API is unavailable."
  spend_aware_usage:
    - "Use a low fixed TRON Nile USDT payment per request."
  when_to_use:
    - "Payment-route integration checks while SunPump upstream POST returns 405"
  request_examples:
    - "POST /providers/sunpump-token-launch-tron/pump-api/ai/agentTokenLaunch"

routing:
  type: proxy

operator:
  network: tron-nile
  currencies:
    usd: ["USDT"]
  recipient: TTX1Us19zqsLXhY39PPR7KRUoMa93s3J3i
  scheme: exact
  protocol: exact
  asset_transfer_method: permit2
  facilitator_url: https://tn-facilitator.bankofai.io
  facilitator_api_key: 2125fd45151833cfb529d1e77885fd46a1244966bc4e83add35b40ba494ea89d
  valid_for_seconds: 300

endpoints:
  - method: POST
    path: /pump-api/ai/agentTokenLaunch
    description: "Configured SunPump launch route; upstream POST currently returns 405"
    metering:
      dimensions:
        - direction: usage
          unit: requests
          scale: 1
          tiers:
            - price_usd: 0.000001
